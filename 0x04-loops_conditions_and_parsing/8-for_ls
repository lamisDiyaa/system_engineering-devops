#!/usr/bin/env bash
# Script to list non-hidden files in the current directory, showing only the part after the first dash

for file in *; do
  # Check if the file name contains a dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
